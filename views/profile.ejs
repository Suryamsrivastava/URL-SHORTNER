<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UrlShortner Profile</title>
    <style>
        /* General Styles */
        body {
            color: #e0e0e0;
            background-color: #121212;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Header */
        header {
            background-color: #1e1e1e;
            padding: 20px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(255, 255, 255, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header h1 {
            font-size: 24px;
            color: #e0e0e0;
            margin: 0;
        }

        nav {
            display: flex;
            gap: 15px;
        }

        nav a {
            color: white;
            text-decoration: none;
            background-color: #007acc;
            padding: 8px 12px;
            border-radius: 5px;
            font-size: 14px;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        nav a:hover {
            background-color: #005f99;
            transform: scale(1.05);
        }

        /* Profile Container */
        .profile-container {
            width: 90%;
            max-width: 600px;
            background: #1e1e1e;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(255, 255, 255, 0.1);
            text-align: center;
            margin: 40px auto;
        }

        /* Profile Avatar */
        .profile-avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            overflow: hidden;
            margin: 0 auto 15px;
        }

        .profile-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Profile Name */
        .profile-name h2 {
            color: #4db6ac;
            margin-bottom: 10px;
        }

        /* Profile Info */
        .profile-info {
            background-color: #252525;
            padding: 20px;
            border-radius: 8px;
            font-size: 1rem;
            margin-bottom: 20px;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 20px;
            background-color: #1e1e1e;
            color: white;
            margin-top: auto;
            box-shadow: 0 -2px 5px rgba(255, 255, 255, 0.1);
        }

        .reset-password-btn {
            display: inline-block;
            padding: 12px 25px;
            background-color: #007acc;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background-color 0.3s ease, transform 0.2s ease;
            margin: 5px;
        }

        .reset-password-btn:hover {
            background-color: #005f99;
            transform: scale(1.02);
            box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.6);
        }

        .del-btn:hover {
            background-color: red;
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            header {
                flex-direction: column;
                text-align: center;
            }

            nav {
                margin-top: 10px;
            }

            .profile-avatar {
                width: 80px;
                height: 80px;
            }

            .profile-name h2 {
                font-size: 1.5rem;
            }

            .profile-info {
                font-size: 0.9rem;
                padding: 15px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>URL Shortener</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/logout">Logout</a>
        </nav>
    </header>

    <main class="profile-container">
        <div class="profile-header">
            <div class="profile-avatar">
                <img src="https://api.dicebear.com/8.x/bottts/png?seed=<%= user.name %>" alt="Profile Avatar">
            </div>
            <div class="profile-name">
                <h2>
                    <%= user.name %>
                </h2>
            </div>
        </div>

        <div class="profile-info">
            <p><strong>Email:</strong>
                <%= user.email %>
            </p>
        </div>
        <div class="btn-container">
            <a href="/url/reset-password" class="reset-password-btn">Change Password</a>
            <a href="/url/deleteUser" class="reset-password-btn del-btn">Delete Account</a>
        </div>
    </main>

    <footer>
        <p>© 2025 URL Shortener. All rights reserved.</p>
    </footer>
</body>

</html>